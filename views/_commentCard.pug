mixin commentCard(comment) 
  .reviews__card
    .reviews__avatar
      img.reviews__avatar-img(src=`/img/users/${comment.user.photo}`, alt=`${comment.user.name}`)
      h6.reviews__user= comment.user.name
    p.reviews__text= comment.comment
